"use client";
import { useEffect, useMemo, useState } from "react";
import { getCurrentAuthSession } from "../../../lib/supabaseAuth";
const statusOptions=["novo","em_contato","confirmado","cancelado","concluido"];
export default function Page(){const [items,setItems]=useState<any[]>([]); const [statusFilter,setStatusFilter]=useState("todos"); const [regionFilter,setRegionFilter]=useState(""); const load=async()=>{const s=await getCurrentAuthSession(); if(!s?.access_token)return; const r=await fetch('/api/admin/agendamentos',{headers:{Authorization:`Bearer ${s.access_token}`}}); if(r.ok) setItems((await r.json()).items||[])}; useEffect(()=>{load();},[]); const filtered=useMemo(()=>items.filter((i)=> (statusFilter==='todos'||i.status===statusFilter)&&`${i.city??''}/${i.state??''}`.toLowerCase().includes(regionFilter.toLowerCase())),[items,statusFilter,regionFilter]); return <div className='mx-auto max-w-6xl px-6 py-10'><h1 className='text-3xl font-bold text-[#123F2A]'>Agendamentos</h1><div className='mt-4 flex gap-3'><select value={statusFilter} onChange={(e)=>setStatusFilter(e.target.value)} className='rounded border p-2'><option value='todos'>Todos</option>{statusOptions.map(s=><option key={s} value={s}>{s}</option>)}</select><input value={regionFilter} onChange={(e)=>setRegionFilter(e.target.value)} placeholder='Cidade/UF' className='rounded border p-2'/></div><div className='mt-6 space-y-4'>{filtered.map((i)=><Card key={i.id} item={i} onSaved={load}/>)}</div></div>}
function Card({item,onSaved}:{item:any;onSaved:()=>void}){const [status,setStatus]=useState(item.status); const [notes,setNotes]=useState(item.internal_notes??""); async function save(){const s=await getCurrentAuthSession(); if(!s?.access_token)return; await fetch(`/api/admin/agendamentos/${item.id}`,{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${s.access_token}`},body:JSON.stringify({status,internal_notes:notes})}); onSaved();} return <div className='rounded-2xl border bg-white p-4'><p className='font-semibold'>{item.name} • {item.request_type}</p><p className='text-sm'>{item.email} | {item.phone} | {item.city}/{item.state}</p><p className='text-sm'>Visita: {item.preferred_date ?? '-'} às {item.preferred_time ?? '-'}</p><p className='text-sm'>{item.message}</p><div className='mt-2 flex gap-2'><select value={status} onChange={(e)=>setStatus(e.target.value)} className='rounded border p-1'>{statusOptions.map((s)=><option key={s} value={s}>{s}</option>)}</select><input value={notes} onChange={(e)=>setNotes(e.target.value)} className='flex-1 rounded border p-1' placeholder='Observações internas'/><button onClick={save} className='rounded bg-[#2E7D32] px-3 py-1 text-white'>Salvar</button></div></div>}
