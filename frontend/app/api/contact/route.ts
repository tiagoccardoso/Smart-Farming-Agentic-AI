import { NextRequest, NextResponse } from "next/server";
const VISIT_TYPES = new Set(["visita_agricultura_organica", "conversao_propriedade_organica"]);
const REQUEST_TYPES = new Set(["consultoria_geral", "revisao_caso_agricola", ...VISIT_TYPES]);
function getSupabaseConfig(){const supabaseUrl=process.env.NEXT_PUBLIC_SUPABASE_URL; const anonKey=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; if(!supabaseUrl||!anonKey) throw new Error(); return {supabaseUrl:supabaseUrl.replace(/\/$/,""),anonKey};}
export async function POST(request: NextRequest){try{const b=await request.json(); const name=String(b?.name??"").trim(); const email=String(b?.email??"").trim(); const phone=String(b?.phone??"").trim(); const city=String(b?.city??"").trim(); const state=String(b?.state??"").trim(); const preferredDate=String(b?.preferredDate??"").trim(); const preferredTime=String(b?.preferredTime??"").trim(); const requestType=String(b?.requestType??"").trim(); const message=String(b?.message??"").trim(); if(!name||!REQUEST_TYPES.has(requestType)) return NextResponse.json({error:"Dados inválidos."},{status:400}); if(VISIT_TYPES.has(requestType)&&(!phone||!city||!state||!preferredDate||!preferredTime)) return NextResponse.json({error:"Telefone, cidade, estado, dia e horário são obrigatórios para agendamento de visita."},{status:400}); const {supabaseUrl,anonKey}=getSupabaseConfig(); const res=await fetch(`${supabaseUrl}/rest/v1/specialist_visit_requests`,{method:"POST",headers:{apikey:anonKey,Authorization:`Bearer ${anonKey}`,"Content-Type":"application/json",Prefer:"return=minimal"},body:JSON.stringify({name,email:email||null,phone:phone||null,city:city||null,state:state||null,preferred_date:preferredDate||null,preferred_time:preferredTime||null,request_type:requestType,message:message||null})}); if(!res.ok) return NextResponse.json({error:"Não foi possível salvar sua solicitação."},{status:500}); return NextResponse.json({message:"Solicitação recebida. A especialista entrará em contato para confirmar a visita."});}catch{return NextResponse.json({error:"Erro ao enviar solicitação."},{status:500});}}
